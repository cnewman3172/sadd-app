-- Add administrative prerequisite flags
ALTER TABLE "User"
  ADD COLUMN IF NOT EXISTS "vmisRegistered" BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS "volunteerAgreement" BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS "saddSopRead" BOOLEAN NOT NULL DEFAULT FALSE;

