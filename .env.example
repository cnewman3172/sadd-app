DATABASE_URL=postgresql://sadd:password@db:5432/sadd
JWT_SECRET=changeme
NEXT_PUBLIC_APP_URL=http://localhost:3000
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
CF_TUNNEL_TOKEN=
OSRM_URL=https://router.project-osrm.org
NOMINATIM_URL=https://nominatim.openstreetmap.org
# Geocoding bias and limits
GEO_COUNTRYCODES=us
# Viewbox (left,top,right,bottom) used to bound searches (set to Alaska by default)
GEO_BIAS_VIEWBOX=-179.231086,71.5388,-129.9795,51.2097
# Allowed regions (editable):
# - Comma-separated state codes: e.g., "AK" or "AK,WA"
GEO_ALLOW_STATES=AK
# - Comma-separated state names matching Nominatim's address.state
GEO_ALLOW_STATE_NAMES=Alaska
# - Comma-separated ISO3166-2 region codes, e.g., "US-AK"
GEO_ALLOW_ISO=US-AK
GEOCODE_LIMIT=10
RATE_LIMIT_PER_MINUTE=60
PING_MIN_INTERVAL_MS=3000
AUTH_RATE_LIMIT_PER_MIN=20
GEOCODE_RATE_PER_MIN=60
ETA_RATE_PER_MIN=120
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
# SMTP (Brevo/SendGrid/Mailgun/etc.) for password reset emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="SADD <no-reply@yourdomain.com>"
SMTP_DEBUG=0
SADD_ORG_NAME=Soldiers Against Drunk Driving
SADD_COPYRIGHT=© 2025 Arctic Aura Designs, Soldiers Against Drunk Driving
POSTGRES_PASSWORD=please-change-me
SETUP_KEY=one-time-setup-key
